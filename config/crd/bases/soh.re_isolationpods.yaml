---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: isolationpods.soh.re
spec:
  group: soh.re
  names:
    kind: IsolationPod
    listKind: IsolationPodList
    plural: isolationpods
    shortNames:
    - ipod
    singular: isolationpod
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.serviceName
      name: Service
      type: string
    - jsonPath: .spec.podName
      name: Pod
      type: string
    - jsonPath: .status.inUse
      name: In-Use
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IsolationPod represents a single pod instance managed for connection
          isolation
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IsolationPodSpec defines the desired state of IsolationPod
            properties:
              podIP:
                description: PodIP is the IP address of the pod
                type: string
              podName:
                description: PodName is the name of the underlying Kubernetes pod
                type: string
              port:
                description: Port is the port the pod is listening on
                format: int32
                type: integer
              serviceName:
                description: ServiceName is the name of the DedicatedService this
                  pod belongs to
                type: string
            required:
            - podName
            - port
            - serviceName
            type: object
          status:
            description: IsolationPodStatus defines the observed state of IsolationPod
            properties:
              connectionStartTime:
                description: ConnectionStartTime is when the current connection started
                  (if InUse)
                format: date-time
                type: string
              inUse:
                description: InUse indicates if this pod is currently serving a connection
                type: boolean
              lastUsedTime:
                description: LastUsedTime is when this pod was last used
                format: date-time
                type: string
              phase:
                description: Phase indicates the lifecycle phase of this isolation
                  pod
                type: string
            required:
            - inUse
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
